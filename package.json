{
  "name": "open-game-forge",
  "version": "0.0.1",
  "private": true,
  "description": "Agent-native 2D game editor — Codex CLI + Godot/Unity/WebGL",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build:contracts": "npm -w @ogf/contracts run build",
    "dev:daemon": "npm -w @ogf/daemon run dev",
    "dev:web": "npm -w @ogf/web run dev",
    "dev": "npm-run-all --parallel dev:daemon dev:web",
    "build": "npm-run-all build:contracts && npm -w @ogf/daemon run build && npm -w @ogf/web run build"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5",
    "typescript": "^5.6.3"
  }
}
